import { ProgramFilterPipe } from './program-filter.pipe';

describe('ProgramFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProgramFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
