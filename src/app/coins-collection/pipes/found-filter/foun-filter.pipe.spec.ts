import { FoundFilterPipe } from './found-filter.pipe';

describe('FounFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FoundFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
