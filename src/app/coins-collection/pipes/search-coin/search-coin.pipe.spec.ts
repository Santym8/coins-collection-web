import { SearchCoinPipe } from './search-coin.pipe';

describe('SearchCoinPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCoinPipe();
    expect(pipe).toBeTruthy();
  });
});
